# Local Setup

## Purpose
This document describes how to set up and run the entire project locally.

The project consists of:
- frontend (Next.js)
- backend (FastAPI + LangGraph)
- MongoDB (Docker)

---

## Prerequisites

Make sure the following are installed:

### 1. Node.js
- Recommended: Node 20+
- Verify:
```bash
node -v
npm -v